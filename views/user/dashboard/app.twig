<div class="col s12 m12 l6">
    <div class="card hoverable">
        <div class="card-content">
            <span class="card-title">{{ app.name }}</span>
        </div>
        <div class="card-action">
            <div class="row">
                <div class="col s12">
                    <a class="btn-large waves-effect waves-light" style="width: 100%" href="https://gum.co/{{ app.gumroad_id }}" target="_blank">
                        Buy license
                    </a>
                    {# <a class="btn-large blue accent-4 waves-effect waves-light" style="width: 100%" href="{{ app.url }}" target="_blank">
                        Launch
                    </a> #}
                </div>
            </div>
        </div>
    </div>
</div>
